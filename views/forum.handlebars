<div id="forum">
  <div class="container">
    <div class="row">
      <div class="col-md-6 col-md-offset-3">
        <h2>Welcome <span class="profile-name"></span></h2>
      </div>
        <div>
            <button >Create Post</button>
        </div>
    </div>
    <div class="row">
    <div class="col-8 offset-2">
      <div class="card mt-4">
        <div class="card-body">
          <h5 class="card-title">Example Post</h5>
          <p>
            <strong>ID</strong>: {{post.id}}</p>
          <p>
          <p>
            <strong>Title</strong>: {{post.title}}</p>
          <p>
            <strong>Body:</strong> {{post.body}}</p>
            <a href="/">← Back To Home</a>
        </div>
      </div>
    </div>
    </div>
  </div>
</div>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
  <script type="text/javascript">
  $(document).ready(function() {
  // This file just does a GET request to figure out which user is logged in
  // and updates the HTML on the page
  $.get("/api/user_data").then(function(data) {
    $(".profile-name").text(data.name);
  });

  {{!-- $.get("/api/post_data").then(function(data){
      console.log(data)
  }); --}}
});
</script>